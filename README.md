# json-locales-checker

## Usage

```
npx babel-node index.js directoryPath
```

where `directoryPath` is the name of catalogue where `.json` files are. As default `directoryPath` is set to `locales`.
